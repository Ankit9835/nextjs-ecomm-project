'use client'
import LoadingTable from '@/src/components/global/LoadingTable'
import React from 'react'

const loading = () => {
  return (
      <LoadingTable rows={10}/>
  )
}

export default loading
